# bdd-skill — mirrors [tool.poetry.dependencies] in pyproject.toml
# Install: pip install -r requirements.txt
# After install, run: playwright install

pytest~=7.0
pytest-bdd~=7.0
playwright~=1.40
pytest-playwright~=0.3.0
requests>=2.31,<3.0
Jinja2>=3.1,<4.0
jmespath>=1.0,<2.0
